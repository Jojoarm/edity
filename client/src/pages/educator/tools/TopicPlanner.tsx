export type LessonPlanData = {
  lesson: number;
  subject: string;
  topic: string;
  classLevel: string;
  duration: string;
};

const TopicPlanner = () => {
  return <div>TopicPlanner</div>;
};

export default TopicPlanner;
